% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dkge-specs.R
\name{dkge_transport_spec}
\alias{dkge_transport_spec}
\title{Transport specification helper}
\usage{
dkge_transport_spec(
  centroids,
  sizes = NULL,
  medoid = 1L,
  method = c("sinkhorn", "sinkhorn_cpp", "knn"),
  mapper = NULL,
  epsilon = 0.05,
  max_iter = 200L,
  tol = 1e-06,
  lambda_emb = 1,
  lambda_spa = 0.5,
  sigma_mm = 15,
  lambda_size = 0,
  ...
)
}
\arguments{
\item{centroids}{List of subject-specific centroid matrices (P_s x d).}

\item{sizes}{Optional list of cluster sizes (one numeric vector per subject).}

\item{medoid}{Integer index of the medoid subject (default 1).}

\item{method}{Mapper backend. Default "sinkhorn".}

\item{mapper}{Optional prefit mapper specification (advanced use).}

\item{epsilon}{Sinkhorn entropic regularisation parameter.}

\item{max_iter}{Maximum Sinkhorn iterations.}

\item{tol}{Convergence tolerance for Sinkhorn scaling.}

\item{lambda_emb}{Weight on embedding distance in the cost matrix.}

\item{lambda_spa}{Weight on spatial distance in the cost matrix.}

\item{sigma_mm}{Spatial scale (in millimetres) used when spatial coordinates
are available.}

\item{lambda_size}{Weight on size regularisation between clusters.}

\item{...}{Additional fields stored on the spec (e.g., precomputed loadings
or betas).}
}
\value{
Object with class `dkge_transport_spec`.
}
\description{
Builds a validated transport configuration that can be passed to
[dkge_pipeline()] or transport utilities. The helper enforces basic argument
checks and provides sensible defaults for Sinkhorn-based mapping.
}
\examples{
transport <- dkge_transport_spec(centroids = list(matrix(runif(12), 4, 3)))
}
