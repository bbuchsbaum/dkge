% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dkge-trial-subject.R
\name{dkge_trial_subject_chunks}
\alias{dkge_trial_subject_chunks}
\title{Build a trialwise DKGE subject from feature chunks}
\usage{
dkge_trial_subject_chunks(
  chunks,
  design,
  id = NULL,
  omega = NULL,
  effect_precision = NULL,
  split = c("none", "within_cell", "alternate"),
  tol = 1e-08,
  max_chunks = 100000L
)
}
\arguments{
\item{chunks}{Either a list of numeric \verb{T x P_block} matrices or a function
called as \code{chunks(i)} that returns the next matrix and returns \code{NULL} after
the last block. A function source is the memory-bounded path.}

\item{design}{Numeric \verb{T x q} second-stage design matrix. This may be a
one-hot cell design or a general full-rank basis design.}

\item{id}{Optional subject identifier.}

\item{omega}{Optional final voxel/parcel weighting of length total \code{P}, or a
total-\code{P} square matrix. It is applied after all chunks are reduced.}

\item{effect_precision}{Optional direct q-vector of effect precisions.}

\item{split}{Optional split-half sufficient statistic. \code{"within_cell"}
alternates trials within each one-hot cell; \code{"alternate"} alternates all
trial rows and is valid only when both half-designs remain full rank.}

\item{tol}{Numerical tolerance used for rank and one-hot checks.}

\item{max_chunks}{Safety limit for function sources that fail to terminate.}
}
\value{
A \code{dkge_subject} carrying the same OLS sufficient statistics as
\code{\link[=dkge_trial_subject]{dkge_trial_subject()}} (\code{beta}, \code{effect_noise_cov}, \code{residual_variance},
\code{residual_df}, \code{effect_n}, optional \code{split_betas} / \code{omega}). Extra
fields: \code{input_type = "trialwise_chunks"}, \code{n_trials},
\code{effect_information} (\verb{X'X}), \code{residual_sum_squares}, and \code{error_model}
(\code{storage = "feature_chunked"}, \code{n_chunks}, ...). \code{noise_trace} is not
stored; \code{\link[=dkge_fit]{dkge_fit()}} recomputes it from \code{residual_variance} so spatial
and voxel weights are honoured. The full trialwise response is not stored.
}
\description{
Reduces successive \verb{T x P_block} trialwise matrices without retaining a full
\verb{T x P} response matrix. Each block is reduced with \code{\link[=dkge_trial_subject]{dkge_trial_subject()}},
then the q-space coefficients and per-feature residual statistics are joined
into one \code{\link[=dkge_subject]{dkge_subject()}} record. The returned object retains one \verb{T x q}
design and one \verb{q x P} beta matrix; it never forms a \verb{P x P} matrix.
}
\examples{
set.seed(1)
X <- model.matrix(~ 0 + factor(rep(1:3, each = 4)))
blocks <- list(matrix(rnorm(12 * 2), 12, 2),
               matrix(rnorm(12 * 3), 12, 3))
subject <- dkge_trial_subject_chunks(blocks, X)
}
