% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dkge-trial-subject.R
\name{dkge_trial_subject}
\alias{dkge_trial_subject}
\title{Build a DKGE subject from trialwise beta maps}
\usage{
dkge_trial_subject(
  y,
  design,
  id = NULL,
  omega = NULL,
  effect_precision = NULL,
  split = c("none", "within_cell", "alternate"),
  tol = 1e-08
)
}
\arguments{
\item{y}{Numeric \verb{T x P} matrix of trialwise beta maps.}

\item{design}{Numeric \verb{T x q} second-stage design matrix. This may be a
one-hot cell design or a general full-rank basis design.}

\item{id}{Optional subject identifier.}

\item{omega}{Optional voxel/parcel weighting passed to \code{\link[=dkge_subject]{dkge_subject()}}.}

\item{effect_precision}{Optional direct q-vector of effect precisions.}

\item{split}{Optional split-half sufficient statistic. \code{"within_cell"}
alternates trials within each one-hot cell; \code{"alternate"} alternates all
trial rows and is valid only when both half-designs remain full rank.}

\item{tol}{Numerical tolerance used for rank and one-hot checks.}
}
\value{
A \code{dkge_subject} carrying second-stage sufficient statistics.
}
\description{
Fits the second-stage model \verb{Y = X B + E} without constructing any
voxel-by-voxel covariance. The returned subject contains the q-by-P effect
estimates, \verb{(X'X)^-1}, per-column residual variances, and (for a one-hot
design) per-effect trial counts. These are sufficient for count weighting and
analytic removal of the finite-trial noise moment in \code{\link[=dkge_fit]{dkge_fit()}}.
}
\examples{
set.seed(1)
X <- model.matrix(~ 0 + factor(rep(1:3, each = 4)))
Y <- X \%*\% matrix(rnorm(3 * 5), 3, 5) + matrix(rnorm(12 * 5), 12, 5)
subject <- dkge_trial_subject(Y, X, id = "s1")
}
