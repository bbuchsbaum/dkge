% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dkge-effect-grid.R
\name{dkge_effect_grid}
\alias{dkge_effect_grid}
\title{Construct a DKGE global effect grid}
\usage{
dkge_effect_grid(factors, scope = NULL, block_factors = NULL, sep = ":")
}
\arguments{
\item{factors}{Named list of factor specifications. Each entry may be a
design-kernel factor spec, a vector of level labels, or a scalar level
count.}

\item{scope}{Named character vector or list assigning each factor to
`"within"` or `"between"`. Defaults to `"within"` for all factors. Scope
is term metadata used by contrast routing; it does not alter the kernel.}

\item{block_factors}{Optional factor names that should define independent
blocks when passed to [design_kernel()].}

\item{sep}{Separator used when constructing cell labels.}
}
\value{
A `dkge_effect_grid` object with factor specs, scopes, cells, and
  labels.
}
\description{
Construct a DKGE global effect grid
}
\details{
Passing the resulting grid to [design_kernel()] preserves its
  globally named cell order. For three or more factors, note that
  `design_kernel(terms = NULL)` includes the main effects and the single full
  interaction only. Supply `terms` explicitly when two-way interactions are
  part of the scientific model.
}
