% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dkge-target.R
\name{dkge_make_target}
\alias{dkge_make_target}
\title{Build a subject-by-feature target for between-subject DKGE models}
\usage{
dkge_make_target(
  fit = NULL,
  type = c("matrix", "component_scores", "transported_maps"),
  Y = NULL,
  contrast = NULL,
  contrast_obj = NULL,
  transport = NULL,
  values = NULL,
  loadings = NULL,
  centroids = NULL,
  sizes = NULL,
  medoid = NULL,
  mapper = NULL,
  crossfit = c("none", "analytic", "loso", "kfold"),
  feature_ids = NULL,
  subject_ids = NULL,
  coverage = NULL,
  precision = NULL,
  subject_weights = NULL,
  feature_weights = NULL,
  geometry = NULL,
  provenance = NULL,
  ...
)
}
\arguments{
\item{fit}{Optional \code{dkge} object used to derive DKGE targets.}

\item{type}{Target type. \code{"matrix"} wraps a supplied matrix, \code{"component_scores"}
averages subject component expressions, and \code{"transported_maps"} builds
a common-space matrix from contrast values.}

\item{Y}{Optional subject-by-feature matrix.}

\item{contrast}{Optional DKGE contrast used when \code{type = "transported_maps"}.}

\item{contrast_obj}{Optional precomputed \code{dkge_contrasts} object.}

\item{transport}{Optional \code{dkge_transport_spec} used for transported maps.}

\item{values}{Optional subject values. May be a matrix or a list of vectors.}

\item{loadings}{Optional subject loading matrices used by transport.}

\item{centroids, sizes, medoid, mapper}{Transport inputs overriding \code{transport}.}

\item{crossfit}{Cross-fitting method used when computing \code{contrast}.}

\item{feature_ids, subject_ids}{Optional identifiers.}

\item{coverage}{Optional coverage matrix/vector aligned with \code{Y}.}

\item{precision}{Optional precision matrix/vector aligned with \code{Y}.}

\item{subject_weights}{Optional subject reliability weights.}

\item{feature_weights}{Optional feature reliability weights.}

\item{geometry}{Optional feature geometry metadata.}

\item{provenance}{Optional provenance metadata.}

\item{...}{Additional arguments forwarded to contrast/transport helpers.}
}
\value{
Object of class \code{dkge_target}.
}
\description{
Creates the explicit boundary object between the DKGE representation stage
and between-subject multivariate models. The returned target stores a
subject-by-feature matrix plus optional coverage, precision, and weighting
metadata.
}
\details{
For \code{type = "transported_maps"}, every contrast in \code{contrast_obj} becomes a
block of columns of \code{Y}; earlier versions kept only the first contrast when
\code{centroids} was \code{NULL} and silently dropped the rest. Feature identifiers are
the contrast name joined to the within-contrast feature label by \code{":"}
(\code{"c1:v1"}, or \code{"c1:1"}, \code{"c1:2"}, ... when the values carry no column
names), so they are stable across calls and identical on the centroid and
no-centroid paths. Unnamed contrasts are labelled \code{contrast1}, \code{contrast2},
and so on.
}
