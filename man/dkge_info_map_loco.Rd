% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dkge-info-maps.R
\name{dkge_info_map_loco}
\alias{dkge_info_map_loco}
\title{Group-LOCO anchor importance (zeroing proxy)}
\usage{
dkge_info_map_loco(
  fit,
  clf,
  renderer,
  neighborhoods = NULL,
  k_nn = 32,
  aggregate = c("mean", "sum")
)
}
\arguments{
\item{fit}{Fitted \code{dkge} object.}

\item{clf}{Cross-fitted classifier from \code{\link[=dkge_cv_train_latent_classifier]{dkge_cv_train_latent_classifier()}}.}

\item{renderer}{Renderer produced by \code{\link[=dkge_build_renderer]{dkge_build_renderer()}}.}

\item{neighborhoods}{Optional list of integer vectors defining anchor
neighborhoods. When \code{NULL}, the function derives them from the renderer's
anchor graph.}

\item{k_nn}{When neighborhoods are not supplied, limits the automatically
derived neighborhood size.}

\item{aggregate}{Aggregation method across subjects: \code{"mean"} or \code{"sum"}.}
}
\value{
A list with LOCO scores per anchor, per-subject anchor maps, and the
neighborhood definition.
}
\description{
Approximates the influence of each anchor by zeroing its neighborhood in the
subject maps derived from classifier weights and accumulating the lost margin
magnitude across subjects.
}
