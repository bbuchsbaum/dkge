% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dkge-transport.R
\name{dkge_transport_contrasts_to_medoid}
\alias{dkge_transport_contrasts_to_medoid}
\title{Transport subject contrasts to a medoid parcellation}
\usage{
dkge_transport_contrasts_to_medoid(
  fit,
  contrast_obj,
  medoid,
  centroids = NULL,
  loadings = NULL,
  betas = NULL,
  sizes = NULL,
  mapper = NULL,
  method = c("sinkhorn", "sinkhorn_cpp"),
  transport_cache = NULL,
  ...
)
}
\arguments{
\item{fit}{A `dkge` object used to compute the contrasts.}

\item{contrast_obj}{A `dkge_contrasts` result.}

\item{medoid}{Integer index of the reference subject (1-based).}

\item{centroids}{List of subject cluster centroids (each P_s × 3 matrix).}

\item{loadings}{Optional list of subject loadings (P_s × r).}

\item{betas}{Optional list of subject betas used to recompute loadings.}

\item{sizes}{Optional list of cluster masses.}

\item{mapper}{Optional mapper specification created by [dkge_mapper_spec()].}

\item{method}{Backwards-compatible alias; currently only "sinkhorn" is
supported.}

\item{transport_cache}{Optional cache from [dkge_prepare_transport()]. When
supplied, cached operators are reused for every contrast.}

\item{...}{Additional parameters passed when building the default mapper
specification.}
}
\value{
Named list of transport results (one per contrast) with an attached
  `cache` element for reuse.
}
\description{
Transport subject contrasts to a medoid parcellation
}
