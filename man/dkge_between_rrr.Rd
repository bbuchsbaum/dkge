% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dkge-between-rrr.R
\name{dkge_between_rrr}
\alias{dkge_between_rrr}
\title{Reduced-rank regression on a DKGE subject target}
\usage{
dkge_between_rrr(
  target,
  design,
  rank = NULL,
  weights = c("none", "target"),
  feature_mask = NULL,
  tol = 1e-08
)
}
\arguments{
\item{target}{A \code{dkge_target} from \code{\link[=dkge_make_target]{dkge_make_target()}} or a numeric matrix.}

\item{design}{A \code{dkge_subject_model} from \code{\link[=dkge_subject_model]{dkge_subject_model()}} or a numeric
model matrix.}

\item{rank}{Reduced rank. Defaults to the maximum estimable rank.}

\item{weights}{\code{"none"}, \code{"target"}, or a list with optional \code{subject} and
\code{feature} numeric weights.}

\item{feature_mask}{Optional logical vector selecting target features.}

\item{tol}{Numerical tolerance for rank checks; a single positive finite
number. It is stored on the fit and reused for every refit performed by
\code{\link[=dkge_between_permute]{dkge_between_permute()}}, so a design accepted here is also accepted by the
permutation machinery.}
}
\value{
Object of class \code{dkge_between_rrr}.
}
\description{
Fits a directional multivariate second-level model
\deqn{Y = X B + E,\quad rank(B) <= r}
using a compact SVD of the fitted design-space response. This is intended for
subject-level factors, traits, and covariates after DKGE has produced a
subject-by-feature target.
}
\examples{
set.seed(1)
n <- 12
dat <- data.frame(
  subject_id = paste0("s", seq_len(n)),
  group = factor(rep(c("A", "B"), length.out = n)),
  trait = scale(rnorm(n), center = TRUE, scale = FALSE)[, 1]
)
design <- dkge_subject_model(~ group * trait, dat)
Y <- design$X \%*\% matrix(c(0, 1, 0.5, -0.2), ncol(design$X), 1)
target <- dkge_make_target(Y = Y, subject_ids = dat$subject_id)
dkge_between_rrr(target, design, rank = 1)
}
