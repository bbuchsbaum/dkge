% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dkge-anchor-fit.R
\name{dkge_anchor_contrast_from_prototypes}
\alias{dkge_anchor_contrast_from_prototypes}
\title{Build an anchor contrast from prototype feature sets}
\usage{
dkge_anchor_contrast_from_prototypes(
  anchors,
  positives,
  negatives = NULL,
  sigma = NULL,
  normalize = TRUE
)
}
\arguments{
\item{anchors}{Matrix of anchor coordinates (`L x d`).}

\item{positives}{Matrix (or vector) of positive prototypes in feature space.}

\item{negatives}{Optional matrix (or vector) of negative prototypes.}

\item{sigma}{Optional bandwidth for the prototype kernel. Defaults to the
median distance between anchors and prototypes.}

\item{normalize}{Logical; L2-normalise the resulting contrast.}
}
\value{
Numeric vector of length `L` suitable for [dkge_contrast()].
}
\description{
Build an anchor contrast from prototype feature sets
}
\examples{
anchors <- matrix(rnorm(10 * 4), 10, 4)
pos <- anchors[1:2, , drop = FALSE]
neg <- anchors[3:4, , drop = FALSE]
w <- dkge_anchor_contrast_from_prototypes(anchors, positives = pos, negatives = neg)
length(w)
}
