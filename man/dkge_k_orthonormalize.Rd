% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dkge-procrustes.R
\name{dkge_k_orthonormalize}
\alias{dkge_k_orthonormalize}
\title{Robust K-orthonormalization}
\usage{
dkge_k_orthonormalize(W, K, Kroots = NULL)
}
\arguments{
\item{W}{qxr matrix (columns = basis vectors)}

\item{K}{qxq design kernel (PSD)}

\item{Kroots}{Optional precomputed kernel roots retained for API
compatibility. Orthonormalization is computed from the exact Gram matrix
`t(W) %*% K %*% W`, so null directions of a PSD kernel are not jittered
into artificial metric dimensions.}
}
\value{
qxr matrix with K-orthonormal columns
}
\description{
Ensures the columns of `W` are orthonormal with respect to the
design kernel metric: U^T K U = I.
}
\examples{
K <- diag(5)
W <- matrix(rnorm(10), 5, 2)
U <- dkge_k_orthonormalize(W, K)
# Verify K-orthonormality
round(t(U) \%*\% K \%*\% U, 10)
}
