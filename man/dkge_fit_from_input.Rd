% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dkge-input.R
\name{dkge_fit_from_input}
\alias{dkge_fit_from_input}
\alias{dkge_fit_from_input.dkge_input_anchor}
\alias{dkge_fit_from_input.default}
\title{Fit DKGE from an input descriptor}
\usage{
dkge_fit_from_input(input, ...)

\method{dkge_fit_from_input}{dkge_input_anchor}(input, ...)

\method{dkge_fit_from_input}{default}(input, ...)
}
\arguments{
\item{input}{Object inheriting from `dkge_input`.}

\item{...}{Additional arguments merged into the DKGE fitting call (these are
interpreted as DKGE core options, e.g. `w_method`).}
}
\value{
A fitted `dkge` object.
}
\description{
Dispatches to the appropriate preprocessing pipeline (anchors, raw betas,
etc.) before invoking the DKGE core. Currently supports anchor descriptors
created via [dkge_input_anchor()].
}
\examples{
\donttest{
set.seed(1)
features_list <- list(
  s1 = matrix(rnorm(20 * 4), 20, 4),
  s2 = matrix(rnorm(25 * 4), 25, 4),
  s3 = matrix(rnorm(22 * 4), 22, 4)
)
K_item_list <- lapply(features_list, function(X) tcrossprod(matrix(rnorm(nrow(X) * 3), nrow(X), 3)))
input <- dkge_input_anchor(features_list, K_item_list,
                           anchors = list(L = 6, method = "dkpp", seed = 1),
                           dkge_args = list(rank = 2))
fit <- dkge_fit_from_input(input)
fit$rank
}
}
