% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dkge-sim.R
\name{dkge_sim_toy}
\alias{dkge_sim_toy}
\title{Simulate toy DKGE datasets with known factorial structure}
\usage{
dkge_sim_toy(
  factors,
  terms = NULL,
  active_terms,
  r_per_term = NULL,
  S = 3,
  P = 10,
  snr = 8,
  noise_scales = NULL,
  seed = 1L,
  contrasts_type = c("helmert", "sum")
)
}
\arguments{
\item{factors}{Named list describing the experimental factors (as for
\code{\link[=design_kernel]{design_kernel()}}), e.g. \code{list(A = list(L = 2), B = list(L = 3))}.}

\item{terms}{Optional list of character vectors specifying which terms to
include in the kernel. Defaults to the full factorial set used by
\code{\link[=design_kernel]{design_kernel()}}.}

\item{active_terms}{Character vector of term names to activate (e.g.,
\code{c("A", "B", "A:B")}). These must be present in the kernel.}

\item{r_per_term}{Named integer vector giving how many latent columns to draw
within each active term. Defaults to one column per term.}

\item{S}{Number of subjects.}

\item{P}{Either a single integer (clusters/voxels per subject) or a length-\code{S}
integer vector.}

\item{snr}{Target Frobenius SNR (signal / noise) per subject. Scalar or
length-\code{S}.}

\item{noise_scales}{Optional length-\code{S} multipliers applied to the noise
standard deviation for each subject.}

\item{seed}{RNG seed for reproducibility.}

\item{contrasts_type}{Factor-contrast system, one of \code{"helmert"} or \code{"sum"}.}
}
\value{
A list with the following entries:
\describe{
\item{B_list}{List of \verb{q x P_s} beta matrices.}
\item{X_list}{List of subject-level design rulers (identity matrices).}
\item{K}{\verb{q x q} design kernel.}
\item{info}{Design metadata returned by \code{\link[=design_kernel]{design_kernel()}}.}
\item{U_true}{Ground-truth \verb{q x r_true} component basis (\eqn{K}-orthonormal).}
\item{M_list}{Per-subject spatial patterns (\verb{P_s x r_true}).}
\item{active_cols}{Named list mapping each active term to the selected
column indices in the effect basis.}
\item{subject_ids}{Character vector of subject identifiers.}
}
}
\description{
Generates subject-level beta matrices \eqn{B_s} (\verb{q x P_s}) in effect space with
user-selected active design terms, controlled signal-to-noise ratio (SNR), and
per-subject noise levels. The routine also returns the per-subject design
'rulers' (typically identity matrices) and the design kernel \eqn{K} so the
synthetic dataset can be fed directly to \code{\link[=dkge_fit]{dkge_fit()}}. Components are planted
inside chosen effect blocks, then \eqn{K}-orthonormalised to provide a ground
truth subspace.
}
\details{
Construction follows the model
\deqn{B_s = U_{true} M_s^\top + E_s,}
where \code{U_true} lies inside the requested design-term blocks, \code{M_s} encodes the
subject-specific spatial loadings, and \code{E_s} is Gaussian noise adjusted to hit
the requested Frobenius SNR.
}
\examples{
# Generate 3-subject toy data with 2x3 factorial design
toy <- dkge_sim_toy(
  factors = list(A = list(L = 2), B = list(L = 3)),
  active_terms = c("A", "B"),
  S = 3, P = 20, snr = 5
)
length(toy$B_list)   # 3 subjects
dim(toy$B_list[[1]]) # 5 effects x 20 clusters
dim(toy$K)           # 5x5 kernel
}
