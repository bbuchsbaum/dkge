% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dkge-aggregate.R
\name{dkge_aggregate_stat}
\alias{dkge_aggregate_stat}
\title{Evaluate aggregate-fit statistics}
\usage{
dkge_aggregate_stat(
  fit,
  statistic = c("singular_value", "salience_contrast", "component_score_contrast",
    "contrast_score", "between_group_contrast"),
  component = 1L,
  contrast = NULL,
  ...
)
}
\arguments{
\item{fit}{A \code{dkge_aggregate_fit}.}

\item{statistic}{Built-in statistic name or a function accepting \code{fit}.
\code{"singular_value"} returns the selected component singular value.
\code{"salience_contrast"} projects the selected component salience vector onto
\code{contrast}. \code{"component_score_contrast"} applies the same contrast to the
component's singular-value-scaled salience vector. \code{"contrast_score"} is a
legacy alias for \code{"salience_contrast"}. \code{"between_group_contrast"} is a
bridge-analysis alias for \code{"component_score_contrast"} and should be
supplied a group-by-within row contrast.}

\item{component}{Component index.}

\item{contrast}{Numeric row contrast. Required for every statistic except
\code{"singular_value"}, i.e. for \code{"salience_contrast"},
\code{"component_score_contrast"}, \code{"contrast_score"}, and
\code{"between_group_contrast"}. May be named, in which case it is reordered to
the aggregate row IDs.}

\item{...}{Additional arguments passed to a user-supplied statistic
function. Built-in statistics take only \code{component} and \code{contrast}, so any
other argument reaching \code{...} is reported as an error rather than being
silently ignored.}
}
\value{
Numeric scalar statistic. Contrast statistics are returned
\strong{signed}; the sign is only interpretable relative to the component
orientation of the fit the statistic is evaluated on.
\code{\link[=dkge_aggregate_bootstrap]{dkge_aggregate_bootstrap()}} orients resampled fits with
\code{\link[=dkge_aggregate_align]{dkge_aggregate_align()}} before taking the statistic;
\code{\link[=dkge_aggregate_permute]{dkge_aggregate_permute()}} does not (null draws stay unaligned; two-sided
\code{abs()} absorbs the arbitrary component sign).
\code{\link[=dkge_aggregate_bootstrap]{dkge_aggregate_bootstrap()}} reports a percentile interval on the signed
statistic so that it can legitimately contain zero.
}
\description{
Aggregate statistics operate on a cell-mean decomposition, not on a
subject-level second-level model. In particular, \code{"between_group_contrast"}
is a convenience bridge statistic: it applies a supplied group-by-cell row
contrast to the selected component's singular-value-scaled aggregate
saliences. It should not be interpreted as a replacement for
\code{\link[=dkge_between_rrr]{dkge_between_rrr()}} or \code{\link[=dkge_between_permute]{dkge_between_permute()}} on subject-level targets.
}
\examples{
set.seed(1)
values <- lapply(1:4, function(i) {
  M <- matrix(rnorm(2 * 3), 2, 3)
  dimnames(M) <- list(c("c1", "c2"), paste0("f", 1:3))
  M
})
names(values) <- paste0("s", 1:4)
subject_data <- data.frame(subject_id = names(values),
                           grp = c("A", "A", "B", "B"))
target <- dkge_aggregate_target(values, subject_data, group_vars = "grp")
fit <- dkge_aggregate_fit(target, rank = 1)
dkge_aggregate_stat(fit, "singular_value")
}
