% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dkge-latent-utils.R
\name{dkge_project_clusters_to_latent}
\alias{dkge_project_clusters_to_latent}
\title{Project subject clusters into the DKGE latent space}
\usage{
dkge_project_clusters_to_latent(fit)
}
\arguments{
\item{fit}{Fitted object of class `dkge` containing `Btil`, `K`, and `U`.}
}
\value{
A list of length `S` (number of subjects). Element `s` is a
  `P_s x r` matrix holding the latent representation of subject `s`'s
  clusters.
}
\description{
For each subject, computes \eqn{Z_s = B_s^\top K U} so that every row
represents a subject cluster embedded in the \eqn{r}-dimensional DKGE
latent space. These projections are commonly used for training classifiers
or computing Haufe-style encoders.
}
\examples{
\dontrun{
Z_list <- dkge_project_clusters_to_latent(fit)
str(Z_list[[1]])  # P_1 x r matrix
}
}
