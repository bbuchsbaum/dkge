---
phase: 02-fit-layer-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-fit.R
  - tests/testthat/test-fit-helpers.R
autonomous: true

must_haves:
  truths:
    - "dkge_fit() output U is K-orthonormal: t(U) %*% K %*% U = I_r within 1e-8 tolerance"
    - "Pooled Gram matrix equals sum of subject Gram matrices: t(R) %*% R = sum_s X_s^T X_s"
    - "K-orthonormality holds for rank=1, rank=q, and intermediate ranks"
    - "K-orthonormality holds across kernel types (identity, RBF, multi-factor)"
  artifacts:
    - path: "tests/testthat/test-fit.R"
      provides: "K-orthonormality property tests for dkge_fit() output"
      contains: "t(fit$U) %*% fit$K %*% fit$U"
    - path: "tests/testthat/test-fit-helpers.R"
      provides: "Stage-level verification tests for fit pipeline"
      contains: ".dkge_fit_solve"
  key_links:
    - from: "R/dkge-fit-core.R"
      to: "tests/testthat/test-fit.R"
      via: "K-orthonormality assertion on U"
      pattern: "t\\(.*\\$U\\).*%\\*%.*\\$K.*%\\*%.*\\$U"
---

<objective>
Add K-orthonormality property tests to verify dkge_fit() produces mathematically valid bases, and add pooled design verification tests to confirm correct aggregation of subject data.

Purpose: These are the foundational mathematical invariants that must hold for the DKGE algorithm to be correct. K-orthonormality (U^T K U = I) is the key property specified in algo.md, and pooled Gram verification ensures subject data is aggregated correctly before eigendecomposition.

Output: Enhanced test-fit.R with explicit K-orthonormality assertions across multiple configurations; enhanced test-fit-helpers.R with pooled design verification tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fit-layer-correctness/02-RESEARCH.md

# Relevant source files
@R/dkge-fit.R
@R/dkge-fit-core.R
@tests/testthat/test-fit.R
@tests/testthat/test-fit-helpers.R

# Prior phase patterns (for test structure)
@.planning/phases/01-data-kernel-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add K-orthonormality property tests to test-fit.R</name>
  <files>tests/testthat/test-fit.R</files>
  <action>
Add comprehensive K-orthonormality property tests to test-fit.R. These tests verify the fundamental mathematical invariant: t(U) %*% K %*% U = I_r.

Tests to add:

1. **Basic K-orthonormality test** - Verify t(U) %*% K %*% U = I_r for a simple fit with identity kernel
2. **K-orthonormality with RBF kernel** - Use ordinal factor with RBF smoothing (like design_kernel with type="ordinal")
3. **K-orthonormality with multi-factor kernel** - Multiple factors producing a structured kernel
4. **Rank edge cases**:
   - rank=1: Single component case
   - rank=q: Full rank case (all components extracted)
   - rank=q-1: Near-full rank
5. **K-orthonormality with ridge** - Verify property holds when ridge > 0
6. **K-orthonormality with subject weights** - Test w_method="mfa_sigma1" and w_method="energy"

Use tolerance of 1e-8 (established in Phase 1). Use max(abs(UtKU - I)) pattern from test-dkge-loso.R.

Pattern to follow from test-dkge-loso.R line 119-120:
```r
G <- t(out$basis) %*% fit$K %*% out$basis
expect_lt(max(abs(G - diag(r))), 1e-8)
```

For each test:
- Use withr::local_seed() for reproducibility
- Create fixture with make_fit_fixture() or explicit construction
- Call dkge_fit() with appropriate parameters
- Assert K-orthonormality: expect_lt(max(abs(t(fit$U) %*% fit$K %*% fit$U - diag(fit$rank))), 1e-8)

Do NOT modify existing tests - add new test_that() blocks after existing ones.
  </action>
  <verify>
```bash
Rscript -e "devtools::test(filter = 'fit')"
```
All existing tests pass plus new K-orthonormality tests pass.
  </verify>
  <done>At least 6 new test_that() blocks added to test-fit.R, all asserting K-orthonormality holds within 1e-8 tolerance.</done>
</task>

<task type="auto">
  <name>Task 2: Add pooled design verification tests</name>
  <files>tests/testthat/test-fit.R</files>
  <action>
Add tests verifying the pooled design computation is correct. According to algo.md:
- G_pool = sum_s X_s^T X_s
- R^T R = G_pool (R is upper-triangular Cholesky factor)

Tests to add:

1. **Pooled Gram with heterogeneous T_s** - Subjects with different numbers of time points (different T_s values in their design matrices X_s)
2. **Pooled Gram with many subjects** - S=10 subjects to verify accumulation doesn't drift
3. **Row standardization verification** - Verify B_tilde_s = R^T B_s produces expected scaling

These tests build on the existing test at line 16-21 of test-fit.R but add coverage for edge cases.

Pattern to follow (already in test-fit.R):
```r
ruler <- dkge:::.dkge_compute_shared_ruler(fixture$designs)
expect_equal(ruler$G_pool, Reduce(`+`, lapply(fixture$designs, crossprod)))
expect_equal(t(ruler$R) %*% ruler$R, ruler$G_pool, tolerance = 1e-10)
```

Use withr::local_seed() for reproducibility.
  </action>
  <verify>
```bash
Rscript -e "devtools::test(filter = 'fit')"
```
All tests pass including new pooled design verification tests.
  </verify>
  <done>At least 3 new test_that() blocks added verifying pooled design computation correctness.</done>
</task>

<task type="auto">
  <name>Task 3: Verify full test suite and commit</name>
  <files></files>
  <action>
Run the full test suite to verify no regressions were introduced.

```bash
Rscript -e "devtools::test()"
```

All tests must pass. If any failures occur, diagnose and fix before proceeding.

After verification, tests are ready for atomic commits per task.
  </action>
  <verify>
```bash
Rscript -e "devtools::test()" 2>&1 | tail -20
```
Output shows 0 failures, 0 errors. Test count should increase from baseline.
  </verify>
  <done>Full test suite passes with 0 failures. New test count exceeds previous count.</done>
</task>

</tasks>

<verification>
Mathematical invariants verified:
- [ ] K-orthonormality: t(U) %*% K %*% U = I_r for at least 6 configurations
- [ ] Pooled Gram: t(R) %*% R = sum_s X_s^T X_s for heterogeneous subjects
- [ ] All tests use tolerance of 1e-8 (established standard)
- [ ] Tests use withr::local_seed() for reproducibility
</verification>

<success_criteria>
1. At least 9 new test_that() blocks added (6 K-orthonormality + 3 pooled design)
2. All new tests explicitly assert mathematical invariants from algo.md
3. Full test suite passes with 0 failures
4. Tests cover rank edge cases (1, q-1, q) and multiple kernel types
</success_criteria>

<output>
After completion, create `.planning/phases/02-fit-layer-correctness/02-01-SUMMARY.md`
</output>
