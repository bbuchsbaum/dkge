---
phase: 06-integration-s3-contracts
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - R/dkge-fit.R
  - R/dkge-write.R
  - R/dkge-input.R
  - R/dkge-inference.R
  - .Rbuildignore
autonomous: true

must_haves:
  truths:
    - "R CMD check produces 0 errors"
    - "R CMD check produces 0 warnings"
    - "R CMD check produces 0 notes"
  artifacts:
    - path: ".Rbuildignore"
      provides: "Build ignore patterns for hidden files"
      contains: "\\.planning"
    - path: "R/dkge-fit.R"
      provides: "Fixed documentation for undocumented arguments"
      contains: "@param solver"
  key_links:
    - from: "R/dkge-*.R"
      to: "NAMESPACE"
      via: "roxygen2 @importFrom"
      pattern: "@importFrom utils capture.output"
---

<objective>
Fix all R CMD check warnings and notes to achieve 0 errors, 0 warnings, 0 notes compliance.

Purpose: Prepare the package for publication/submission with clean R CMD check output.
Output: Fixed R source files, updated .Rbuildignore, clean R CMD check
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration-s3-contracts/06-RESEARCH.md
@.planning/phases/06-integration-s3-contracts/06-CONTEXT.md

@NAMESPACE
@.Rbuildignore
@R/dkge-fit.R
@R/dkge-write.R
@R/dkge-input.R
@R/dkge-inference.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix R CMD check warnings (documentation mismatches)</name>
  <files>R/dkge-fit.R, R/dkge-input.R, R/dkge-inference.R</files>
  <action>
Fix all documentation-related warnings from R CMD check (from RESEARCH.md analysis):

**1. dkge_fit.R - Add missing @param entries:**
```r
#' @param solver Solver for q-space problem: "pooled" (default) or "jd"
#' @param jd_control Control parameters from [dkge_jd_control()]
#' @param jd_mask Optional mask for JD off-diagonal penalty
#' @param jd_init Optional orthogonal initializer for JD solver
```
Add these to the dkge_fit roxygen block, before @return.

**2. dkge_input.R - Fix \times macro:**
Find `\times` in roxygen comments and replace with either:
- `\\eqn{\\times}` for math context
- `*` for plain text multiplication

**3. dkge-inference.R - Fix as.data.frame.dkge_inference:**
- Add missing @param entries: `row.names` and `optional` (standard data.frame args)
- Remove documented `stringsAsFactors` if not in function signature

**4. Fix Rd syntax warnings:**
Run `devtools::document()` after changes. Check for "Lost braces" warnings in:
- dkge_fit.Rd
- dkge_projector_K.Rd
- dkge_regress.Rd

These typically need `{}` around roxygen code references or escaped braces.

After fixing all issues, run `devtools::document()` to regenerate man/*.Rd files.
  </action>
  <verify>
Run: `Rscript -e "devtools::document(); devtools::check(vignettes = FALSE)" 2>&1 | grep -E "(WARNING|NOTE|Error|checking)"`
No warnings related to documentation
  </verify>
  <done>
All documentation mismatches fixed, no codoc warnings
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix R CMD check notes and finalize compliance</name>
  <files>.Rbuildignore, R/dkge-write.R</files>
  <action>
Fix all notes from R CMD check:

**1. .Rbuildignore - Add hidden files:**
Add these lines to .Rbuildignore:
```
^\.planning$
^repomix-output\.txt$
```

**2. R/dkge-write.R - Add missing import:**
Find uses of `capture.output` and add to the file's roxygen block:
```r
#' @importFrom utils capture.output
```

Or change to explicit namespace call: `utils::capture.output(...)`

**3. Verify all fixes together:**
Run full R CMD check (without vignettes due to known vignette issue):
```r
devtools::check(vignettes = FALSE)
```

**4. If any remaining issues:**
- Check for any `no visible global function definition` notes
- Add @importFrom for any missing imports
- Verify NAMESPACE is regenerated with devtools::document()

Target: 0 errors, 0 warnings, 0 notes (excluding vignette issues which are out of scope for this phase).
  </action>
  <verify>
Run: `Rscript -e "devtools::check(vignettes = FALSE)" 2>&1 | tail -20`
Shows "0 errors | 0 warnings | 0 notes" (or notes only about vignettes)
  </verify>
  <done>
R CMD check passes with 0 errors, 0 warnings, 0 notes (vignettes excluded)
  </done>
</task>

</tasks>

<verification>
- `Rscript -e "devtools::check(vignettes = FALSE)"` completes with 0 errors, 0 warnings, 0 notes
- NAMESPACE regenerated correctly
- All documentation mismatches resolved
</verification>

<success_criteria>
- R CMD check passes with clean output (0/0/0)
- Documentation matches code (no codoc mismatches)
- All hidden files excluded from package build
- All function imports properly declared
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-s3-contracts/06-03-SUMMARY.md`
</output>
