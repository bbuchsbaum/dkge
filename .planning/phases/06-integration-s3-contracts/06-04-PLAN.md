---
phase: 06-integration-s3-contracts
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - R/dkge-data.R
  - R/dkge-fit.R
  - R/dkge-contrast.R
  - R/dkge-inference.R
  - R/dkge-pipeline.R
  - R/dkge-weights.R
  - R/dkge-classify.R
  - R/dkge-transport.R
  - R/dkge-regress.R
  - tests/testthat/test-coverage-gaps.R
autonomous: true

must_haves:
  truths:
    - "All exported functions have runnable @examples (no dontrun except for slow ops)"
    - "Test coverage on exported functions exceeds 80%"
    - "Example audit identifies and fixes all missing/broken examples"
  artifacts:
    - path: "tests/testthat/test-coverage-gaps.R"
      provides: "Additional tests targeting coverage gaps"
      min_lines: 50
    - path: "R/dkge-data.R"
      provides: "Fixed examples for data constructors"
      contains: "@examples"
    - path: "R/dkge-fit.R"
      provides: "Fixed examples for fit functions"
      contains: "@examples"
  key_links:
    - from: "R/*.R"
      to: "man/*.Rd"
      via: "roxygen2 @examples"
      pattern: "@examples"
---

<objective>
Audit and fix all exported function examples, then measure and close test coverage gaps to exceed 80%.

Purpose: Ensure all user-facing functions have working examples and comprehensive test coverage for publication readiness.
Output: Fixed @examples in R/*.R files, coverage gap tests, documented coverage report
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration-s3-contracts/06-CONTEXT.md

@NAMESPACE
@R/dkge-data.R
@R/dkge-fit.R
@R/dkge-pipeline.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix @examples for all exported functions</name>
  <files>R/dkge-data.R, R/dkge-fit.R, R/dkge-contrast.R, R/dkge-inference.R, R/dkge-pipeline.R, R/dkge-weights.R, R/dkge-classify.R, R/dkge-transport.R, R/dkge-regress.R</files>
  <action>
Audit all exported functions for working @examples. For each exported function in NAMESPACE:

1. **Identify missing examples:**
   Run: `grep -L "@examples" R/*.R` to find files without any examples.
   Cross-reference with NAMESPACE exports to identify gaps.

2. **Fix missing examples:**
   Add minimal runnable examples to exported functions lacking them. Style from CONTEXT.md:
   - Minimal runnable: smallest code that demonstrates the function works
   - Use `dkge_sim_toy()` for synthetic data (already available in package)
   - Wrap slow operations (transport, inference with >100 perms) in `\donttest{}`
   - NO `\dontrun{}` except for functions requiring external resources

3. **Priority functions to check:**
   - `dkge_data()`, `dkge_subject()` - data constructors
   - `dkge_fit()`, `dkge_fit_from_input()` - core fit
   - `dkge_contrast()`, `dkge_loso_contrast()` - contrast computation
   - `dkge_infer()`, `dkge_signflip_maxT()` - inference
   - `dkge_pipeline()` - high-level entry point
   - `dkge_weights()`, `dkge_weights_auto()` - weighting
   - `dkge_classify()`, `dkge_regress()` - downstream analysis
   - `design_kernel()`, `dkge_design_kernel()` - kernel construction

4. **Verify examples run:**
   After adding examples, run `devtools::run_examples()` to verify all pass.

Example pattern:
```r
#' @examples
#' # Create toy data
#' toy <- dkge_sim_toy(n_subjects = 3, q = 5, P = 20)
#'
#' # Fit model
#' fit <- dkge_fit(toy$betas, toy$designs, toy$kernel)
#' print(fit)
```
  </action>
  <verify>
Run: `Rscript -e "devtools::run_examples()" 2>&1 | tail -30`
All examples pass (no errors in output)
  </verify>
  <done>
All exported functions have working @examples, devtools::run_examples() passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Measure coverage and close gaps to exceed 80%</name>
  <files>tests/testthat/test-coverage-gaps.R</files>
  <action>
Measure current test coverage and add targeted tests to close gaps:

1. **Measure current coverage:**
   Run: `Rscript -e "covr::package_coverage(type='tests')" > coverage_report.txt`
   Identify functions with < 80% coverage.

2. **Create coverage gap tests:**
   Create `tests/testthat/test-coverage-gaps.R` with tests for uncovered code paths.
   Focus on:
   - Exported functions with low coverage
   - Error handling branches (if statements returning early)
   - Edge case code paths
   - Default argument paths

3. **Target areas likely needing coverage:**
   Based on existing test files, areas that may need more coverage:
   - `dkge_pipeline()` error paths
   - `dkge_weights_*()` edge cases
   - Transport functions with different mapper types
   - Inference with different methods (signflip vs freedman_lane)

4. **Re-measure coverage:**
   After adding tests, re-run coverage to verify > 80%.

Test pattern:
```r
test_that("function handles edge case X", {
  # Setup minimal fixture
  toy <- dkge_sim_toy(n_subjects = 2, q = 3, P = 10)

  # Exercise the uncovered code path
  result <- function_under_test(edge_case_input)

  # Verify behavior

  expect_true(is_valid_result(result))
})
```
  </action>
  <verify>
Run: `Rscript -e "cov <- covr::package_coverage(type='tests'); print(covr::percent_coverage(cov))"`
Coverage percentage exceeds 80%
  </verify>
  <done>
Test coverage on exported functions exceeds 80%, coverage gap tests in place
  </done>
</task>

<task type="auto">
  <name>Task 3: Final verification - examples + coverage + R CMD check</name>
  <files>None (verification only)</files>
  <action>
Final verification that all Phase 6 success criteria are met:

1. **Run examples:**
   `devtools::run_examples()` - all pass

2. **Run tests:**
   `devtools::test()` - all pass

3. **Check coverage:**
   `covr::package_coverage()` - exceeds 80%

4. **R CMD check:**
   `devtools::check(vignettes = FALSE)` - 0 errors, 0 warnings, 0 notes

5. **Document results:**
   Record coverage percentage and any remaining notes in summary.

This task verifies the integration of all Phase 6 work.
  </action>
  <verify>
All four commands complete successfully:
- Examples: no errors
- Tests: all pass
- Coverage: > 80%
- R CMD check: 0/0/0
  </verify>
  <done>
Phase 6 success criteria verified: pipeline works, S3 methods dispatch, R CMD check clean, examples work, coverage > 80%
  </done>
</task>

</tasks>

<verification>
- `devtools::run_examples()` completes without errors
- `covr::package_coverage()` reports > 80% on exported functions
- `devtools::check(vignettes = FALSE)` reports 0 errors, 0 warnings, 0 notes
- All exported functions in NAMESPACE have @examples
</verification>

<success_criteria>
- All exported functions have working @examples (no \dontrun except for external resources)
- Test coverage on exported functions exceeds 80%
- All Phase 6 success criteria verified in final check
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-s3-contracts/06-04-SUMMARY.md`
</output>
