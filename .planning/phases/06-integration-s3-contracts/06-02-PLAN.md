---
phase: 06-integration-s3-contracts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-s3-contracts.R
  - tests/testthat/test-print-methods.R
autonomous: true

must_haves:
  truths:
    - "All exported S3 print methods return object invisibly"
    - "predict.dkge returns projected coordinates matrix"
    - "as.data.frame methods return valid data.frames with expected columns"
  artifacts:
    - path: "tests/testthat/test-s3-contracts.R"
      provides: "S3 method contract verification tests"
      min_lines: 60
    - path: "tests/testthat/test-print-methods.R"
      provides: "Print method contract tests"
      min_lines: 40
  key_links:
    - from: "tests/testthat/test-s3-contracts.R"
      to: "S3 methods in NAMESPACE"
      via: "method dispatch"
      pattern: "expect_s3_class|expect_identical"
---

<objective>
Create comprehensive S3 method contract tests ensuring all exported methods dispatch correctly and return expected types.

Purpose: Verify the user-facing API behaves consistently with R conventions, catching dispatch issues and output format regressions.
Output: Test files verifying print, predict, and as.data.frame S3 contracts
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration-s3-contracts/06-RESEARCH.md
@.planning/phases/06-integration-s3-contracts/06-CONTEXT.md

@NAMESPACE
@tests/testthat/test-predict-s3.R
@tests/testthat/test-as-data-frame.R
@tests/testthat/helper-fit-fixture.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive print method contract tests</name>
  <files>tests/testthat/test-print-methods.R</files>
  <action>
Create `tests/testthat/test-print-methods.R` to verify all exported print methods follow R conventions.

S3 print methods from NAMESPACE to test:
- print.dkge_classification
- print.dkge_classification_spec
- print.dkge_contrast_validated
- print.dkge_contrasts
- print.dkge_folds
- print.dkge_inference
- print.dkge_inference_spec
- print.dkge_regress
- print.dkge_transport_spec
- print.dkge_weights

For each print method, verify:
1. Method returns object invisibly (result is identical to input)
2. Method produces output (capture.output length > 0)
3. No errors when called on valid object

Use pattern from RESEARCH.md:
```r
test_that("print.CLASS returns invisibly", {
  obj <- make_CLASS_object()
  output <- capture.output(result <- print(obj))
  expect_s3_class(result, "CLASS")
  expect_identical(result, obj)
  expect_true(length(output) > 0)
})
```

Create minimal fixture objects for each class. Reuse existing helpers where possible.
  </action>
  <verify>
Run: `Rscript -e "devtools::test(filter='print-methods')"`
All tests pass, one test per print method (minimum 10 tests)
  </verify>
  <done>
test-print-methods.R exists with 10+ tests covering all exported print methods
  </done>
</task>

<task type="auto">
  <name>Task 2: Create S3 contract tests for predict and as.data.frame</name>
  <files>tests/testthat/test-s3-contracts.R</files>
  <action>
Create `tests/testthat/test-s3-contracts.R` consolidating S3 contract verification for predict and as.data.frame methods.

From NAMESPACE, verify:
- predict.dkge
- predict.dkge_model
- as.data.frame.dkge_classification
- as.data.frame.dkge_contrasts
- as.data.frame.dkge_inference

For predict methods:
1. Returns matrix/list with expected structure
2. Dimensions match input (new subjects x components)
3. Works with newdata list format

For as.data.frame methods from CONTEXT.md decisions:
1. Returns data.frame class
2. Has expected columns (per class contract)
3. Rows correspond to observations

Contracts from CONTEXT.md:
- predict(): Returns projected coordinates - matrix of q-space projections
- as.data.frame(): Wide format - one row per subject, components as columns (for contrasts)

Note: Some tests may already exist in test-predict-s3.R and test-as-data-frame.R. Reference those for patterns but add any missing coverage.
  </action>
  <verify>
Run: `Rscript -e "devtools::test(filter='s3-contracts')"`
All tests pass, minimum 5 test_that blocks
  </verify>
  <done>
test-s3-contracts.R exists with passing tests for predict and as.data.frame contracts
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify S3 method dispatch registration</name>
  <files>tests/testthat/test-s3-contracts.R</files>
  <action>
Add tests to test-s3-contracts.R verifying that S3 methods are properly registered and dispatch correctly:

1. **Method registration check**:
   - Use `methods(print)` to verify print.dkge_* methods are registered
   - Use `methods(predict)` to verify predict.dkge* methods are registered
   - Use `methods(as.data.frame)` to verify as.data.frame.dkge_* methods are registered

2. **Generic dispatch verification**:
   - Create objects of each class
   - Call generic function (print, predict, as.data.frame)
   - Verify correct method was dispatched (by checking output type)

3. **Class inheritance handling**:
   - Verify that objects with multiple classes dispatch to most specific method

This ensures NAMESPACE exports are correct and R's dispatch mechanism finds the methods.
  </action>
  <verify>
Run: `Rscript -e "devtools::test(filter='s3-contracts')"`
All tests pass, at least 8 total test_that blocks
  </verify>
  <done>
test-s3-contracts.R has 8+ tests covering method dispatch and registration
  </done>
</task>

</tasks>

<verification>
- `Rscript -e "devtools::test(filter='print-methods')"` passes
- `Rscript -e "devtools::test(filter='s3-contracts')"` passes
- All exported S3 methods have contract tests
</verification>

<success_criteria>
- test-print-methods.R exists with 10+ tests for print methods
- test-s3-contracts.R exists with 8+ tests for predict/as.data.frame contracts
- All tests pass, no new warnings
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-s3-contracts/06-02-SUMMARY.md`
</output>
