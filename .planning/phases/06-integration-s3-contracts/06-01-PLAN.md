---
phase: 06-integration-s3-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-integration-pipeline.R
autonomous: true

must_haves:
  truths:
    - "dkge_pipeline() completes full workflow with valid multi-subject data"
    - "Pipeline chains fit -> LOSO contrast -> transport -> inference correctly"
    - "Pipeline handles both pre-computed fit and fit-from-scratch modes"
  artifacts:
    - path: "tests/testthat/test-integration-pipeline.R"
      provides: "Integration tests for dkge_pipeline"
      min_lines: 80
  key_links:
    - from: "tests/testthat/test-integration-pipeline.R"
      to: "dkge_pipeline()"
      via: "function calls"
      pattern: "dkge_pipeline"
---

<objective>
Create comprehensive integration tests for `dkge_pipeline()` that exercise the full workflow from data input to inference results.

Purpose: Verify the end-to-end workflow works correctly when all components are chained together, catching integration issues that unit tests miss.
Output: `tests/testthat/test-integration-pipeline.R` with full workflow tests
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-integration-s3-contracts/06-RESEARCH.md
@.planning/phases/06-integration-s3-contracts/06-CONTEXT.md

@tests/testthat/test-pipeline.R
@tests/testthat/helper-fit-fixture.R
@tests/testthat/helper-toy.R
@R/dkge-pipeline.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive pipeline integration tests</name>
  <files>tests/testthat/test-integration-pipeline.R</files>
  <action>
Create a new test file `tests/testthat/test-integration-pipeline.R` with comprehensive integration tests for the full pipeline workflow.

Test scenarios to cover:

1. **Full pipeline from scratch** (fit computed internally):
   - Input: betas, designs, kernel (nominal), contrasts
   - Verify: fit object created, contrasts computed, diagnostics populated

2. **Full pipeline with transport + inference**:
   - Input: pre-computed fit, contrasts, transport config, inference config
   - Verify: all result components populated (fit, diagnostics, contrasts, transport, inference)

3. **LOSO method produces cross-fitted results**:
   - Verify: U_minus list is populated in contrast results
   - Verify: each U_minus has S-1 subjects worth of data

4. **Pipeline error handling**:
   - Missing required arguments (no fit AND no betas/designs/kernel)
   - Invalid contrast dimensions
   - Mismatched subject counts

Use `make_small_fit()` and `make_pipeline_inputs()` helpers from existing helper files. Create any additional helper functions needed at the top of the test file.

Test data dimensions from CONTEXT.md: small synthetic (q=5, P=20) for fast tests.
  </action>
  <verify>
Run: `Rscript -e "devtools::test(filter='integration-pipeline')"`
All tests pass, minimum 5 test_that blocks
  </verify>
  <done>
test-integration-pipeline.R exists with passing tests covering full workflow scenarios
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify pipeline produces valid downstream outputs</name>
  <files>tests/testthat/test-integration-pipeline.R</files>
  <action>
Add additional tests to test-integration-pipeline.R that verify pipeline outputs are consumable by downstream functions:

5. **Contrasts are valid for as.data.frame()**:
   - Extract contrasts from pipeline result
   - Call as.data.frame() on them
   - Verify data.frame has expected structure

6. **Inference results are valid**:
   - Verify p-values are in [0,1] range
   - Verify statistics are finite
   - Verify result structure matches dkge_inference class

7. **Pipeline with classification**:
   - Create classification targets
   - Pass classification spec to pipeline
   - Verify classification result is populated

These tests ensure pipeline outputs integrate with the rest of the package API.
  </action>
  <verify>
Run: `Rscript -e "devtools::test(filter='integration-pipeline')"`
All tests pass, minimum 8 total test_that blocks
  </verify>
  <done>
test-integration-pipeline.R has 8+ passing tests covering full workflow + downstream consumption
  </done>
</task>

</tasks>

<verification>
- `Rscript -e "devtools::test(filter='integration-pipeline')"` passes all tests
- Tests cover: full workflow, transport, inference, error handling, downstream consumption
- No new warnings introduced
</verification>

<success_criteria>
- test-integration-pipeline.R exists with 8+ passing test_that blocks
- Tests exercise dkge_pipeline() through complete workflows
- Pipeline outputs validated for downstream consumption
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-s3-contracts/06-01-SUMMARY.md`
</output>
