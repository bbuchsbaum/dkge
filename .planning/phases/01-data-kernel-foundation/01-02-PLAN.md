---
phase: 01-data-kernel-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-design-kernel.R
  - tests/testthat/test-kernel-invariants.R
autonomous: true

must_haves:
  truths:
    - "design_kernel() output is symmetric for all factor types"
    - "design_kernel() output is positive semidefinite for all factor types"
    - "kernel_roots() reconstruction satisfies Khalf %*% Khalf = K within 1e-8"
    - "kernel_roots() inverse reconstruction satisfies Kihalf %*% K %*% Kihalf = I"
    - "Kernel construction handles edge cases (1x1, 2x2, near-singular) gracefully"
  artifacts:
    - path: "tests/testthat/test-kernel-invariants.R"
      provides: "Mathematical property tests for kernels"
      min_lines: 100
    - path: "tests/testthat/test-design-kernel.R"
      provides: "Enhanced kernel tests with edge cases"
      contains: "1x1 kernel|small kernel"
  key_links:
    - from: "tests/testthat/test-kernel-invariants.R"
      to: "R/design-kernel.R"
      via: "Property-based testing of kernel functions"
      pattern: "isSymmetric|eigen.*symmetric.*TRUE|Khalf.*Khalf"
---

<objective>
Add mathematical invariant tests for kernel construction and root computation

Purpose: Verify that `design_kernel()` always produces symmetric, PSD matrices and that `kernel_roots()` reconstruction is numerically accurate. This ensures mathematical correctness that downstream fit operations depend on.

Output: New test file `test-kernel-invariants.R` with property-based tests, enhanced `test-design-kernel.R` with edge case coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-kernel-foundation/01-RESEARCH.md

@R/design-kernel.R
@tests/testthat/test-design-kernel.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test-kernel-invariants.R with property tests</name>
  <files>tests/testthat/test-kernel-invariants.R</files>
  <action>
Create a new test file `tests/testthat/test-kernel-invariants.R` with property-based tests that verify mathematical invariants across ALL valid kernel configurations:

1. **Symmetry property test across all factor types:**
```r
test_that("design_kernel produces symmetric matrices for all factor types", {
  factor_specs <- list(
    nominal = list(type = "nominal", L = 4),
    ordinal = list(type = "ordinal", L = 4, l = 1.0),
    circular = list(type = "circular", L = 4, l = 1.0),
    continuous = list(type = "continuous", values = c(1, 2, 3, 4))
  )

  for (name in names(factor_specs)) {
    factors <- list(A = factor_specs[[name]])
    K <- design_kernel(factors, basis = "cell", normalize = "none")$K

    expect_true(isSymmetric(K, tol = 1e-10),
                info = paste("Factor type:", name))
  }
})
```

2. **PSD property test across all factor types:**
```r
test_that("design_kernel produces PSD matrices for all factor types", {
  factor_specs <- list(
    nominal = list(type = "nominal", L = 4),
    ordinal = list(type = "ordinal", L = 4, l = 1.0),
    circular = list(type = "circular", L = 4, l = 1.0),
    continuous = list(type = "continuous", values = c(1, 2, 3, 4))
  )

  for (name in names(factor_specs)) {
    factors <- list(A = factor_specs[[name]])
    K <- design_kernel(factors, basis = "cell", normalize = "none")$K

    # Verify PSD (all eigenvalues >= -epsilon)
    eig <- eigen(K, symmetric = TRUE)$values
    expect_true(all(eig >= -1e-10),
                info = paste("Type:", name, "min eigenvalue:", min(eig)))
  }
})
```

3. **kernel_roots reconstruction identity test:**
```r
test_that("kernel_roots reconstruction satisfies Khalf %*% Khalf = K", {
  # Test across multiple kernel sizes and types
  test_cases <- list(
    # Simple diagonal
    list(K = diag(3), name = "identity"),
    # Random PSD
    list(K = {
      withr::local_seed(42)
      L <- matrix(rnorm(5 * 5), 5, 5)
      K <- crossprod(L)
      (K + t(K)) / 2
    }, name = "random_5x5"),
    # From design_kernel
    list(K = design_kernel(list(A = list(L = 4, type = "ordinal", l = 1.0)),
                           basis = "cell", normalize = "none")$K,
         name = "ordinal_kernel")
  )

  for (tc in test_cases) {
    roots <- kernel_roots(tc$K)
    reconstructed <- roots$Khalf %*% roots$Khalf

    expect_equal(reconstructed, tc$K, tolerance = 1e-8,
                 label = paste("Reconstruction for", tc$name))
  }
})
```

4. **kernel_roots inverse reconstruction test:**
```r
test_that("kernel_roots inverse satisfies Kihalf %*% K %*% Kihalf = I", {
  withr::local_seed(123)
  # Generate random well-conditioned PSD matrix
  L <- matrix(rnorm(4 * 4), 4, 4)
  K <- crossprod(L) + 0.1 * diag(4)  # Add ridge for conditioning
  K <- (K + t(K)) / 2

  roots <- kernel_roots(K)
  identity_approx <- roots$Kihalf %*% K %*% roots$Kihalf

  expect_equal(identity_approx, diag(4), tolerance = 1e-8)
})
```

5. **kernel_roots preserves symmetry:**
```r
test_that("kernel_roots output matrices are symmetric", {
  factors <- list(A = list(L = 3, type = "nominal"))
  K <- design_kernel(factors, basis = "cell", normalize = "none")$K

  roots <- kernel_roots(K)

  expect_true(isSymmetric(roots$Khalf, tol = 1e-10))
  expect_true(isSymmetric(roots$Kihalf, tol = 1e-10))
})
```

6. **Multi-factor interaction kernel properties:**
```r
test_that("multi-factor kernels maintain symmetry and PSD", {
  factors <- list(
    A = list(L = 3, type = "nominal"),
    B = list(L = 4, type = "ordinal", l = 1.5)
  )
  terms <- list("A", "B", c("A", "B"))

  K_res <- design_kernel(factors, terms = terms, basis = "cell", normalize = "unit_trace")
  K <- K_res$K

  # Symmetry
  expect_true(isSymmetric(K, tol = 1e-10))

  # PSD
  eig <- eigen(K, symmetric = TRUE)$values
  expect_true(all(eig >= -1e-10),
              info = paste("Min eigenvalue:", min(eig)))

  # Trace normalization check
  expect_equal(sum(diag(K)), 1, tolerance = 1e-10)
})
```

7. **Randomized property test (multiple seeds):**
```r
test_that("kernel_roots is stable across random PSD matrices", {
  for (seed in c(1, 42, 999)) {
    withr::local_seed(seed)

    # Generate random PSD matrix
    q <- sample(3:10, 1)
    L <- matrix(rnorm(q * q), q, q)
    K <- crossprod(L)
    K <- (K + t(K)) / 2

    roots <- kernel_roots(K)

    # Reconstruction check
    reconstructed <- roots$Khalf %*% roots$Khalf
    expect_equal(reconstructed, K, tolerance = 1e-8,
                 info = paste("Seed:", seed, "q:", q))

    # Rank should equal q for full-rank matrices
    expect_equal(roots$rank, q,
                 info = paste("Seed:", seed, "q:", q))
  }
})
```

File should be ~100-150 lines.
  </action>
  <verify>
```bash
Rscript -e "testthat::test_file('tests/testthat/test-kernel-invariants.R')"
```
All property tests pass.
  </verify>
  <done>
- test-kernel-invariants.R exists with >= 100 lines
- Symmetry tests for all factor types (nominal, ordinal, circular, continuous)
- PSD tests for all factor types
- kernel_roots reconstruction test (Khalf %*% Khalf = K)
- kernel_roots inverse test (Kihalf %*% K %*% Kihalf = I)
- Multi-seed stability test
  </done>
</task>

<task type="auto">
  <name>Task 2: Add edge case tests to test-design-kernel.R</name>
  <files>tests/testthat/test-design-kernel.R</files>
  <action>
Enhance `tests/testthat/test-design-kernel.R` with edge case coverage:

1. **Remove deprecated context() call** (line 3) - edition 3 doesn't use it.

2. **Small kernel edge cases:**
```r
test_that("design_kernel handles 1-level factor (1x1 kernel)", {
  factors <- list(A = list(L = 1, type = "nominal"))
  K_res <- design_kernel(factors, basis = "cell", normalize = "none")

  expect_equal(dim(K_res$K), c(1, 1))
  expect_true(K_res$K[1, 1] > 0)  # Should be positive
})

test_that("design_kernel handles 2-level factor (2x2 kernel)", {
  factors <- list(A = list(L = 2, type = "nominal"))
  K_res <- design_kernel(factors, basis = "cell", normalize = "none")

  expect_equal(dim(K_res$K), c(2, 2))
  expect_true(isSymmetric(K_res$K, tol = 1e-10))

  # For nominal, diagonal should equal 1+jitter, off-diagonal should equal jitter
  expect_equal(K_res$K[1, 1], K_res$K[2, 2], tolerance = 1e-10)
})
```

3. **Input validation tests:**
```r
test_that("design_kernel rejects negative rho values", {
  factors <- list(A = list(L = 3, type = "nominal"))
  expect_error(design_kernel(factors, rho = c("A" = -1)),
               "non-negative|rho")
})

test_that("design_kernel rejects missing L for discrete factors", {
  factors <- list(A = list(type = "nominal"))  # Missing L
  expect_error(design_kernel(factors), "L")
})

test_that("design_kernel rejects missing values for continuous factors", {
  factors <- list(A = list(type = "continuous"))  # Missing values
  expect_error(design_kernel(factors), "values")
})

test_that("design_kernel rejects unnamed factors", {
  factors <- list(list(L = 3, type = "nominal"))  # Unnamed
  expect_error(design_kernel(factors), "named")
})
```

4. **kernel_roots edge cases:**
```r
test_that("kernel_roots handles diagonal matrices", {
  K <- diag(c(2, 1, 0.5))
  roots <- kernel_roots(K)

  expect_equal(diag(roots$Khalf), sqrt(c(2, 1, 0.5)), tolerance = 1e-10)
  reconstructed <- roots$Khalf %*% roots$Khalf
  expect_equal(reconstructed, K, tolerance = 1e-10)
})

test_that("kernel_roots handles near-zero eigenvalues with jitter", {
  K <- diag(c(1, 1e-12, 1e-15))
  roots <- kernel_roots(K, jitter = 1e-10)

  # All eigenvalues should be clamped to at least jitter
  expect_true(all(roots$evals >= 1e-10))
  expect_equal(roots$n_clamped, 2L)
})

test_that("kernel_roots warns for asymmetric input", {
  K <- matrix(c(1, 0.5, 0.6, 1), 2, 2)  # Asymmetric
  expect_warning(kernel_roots(K), "symmetric")
})
```

5. **Circular factor edge case:**
```r
test_that("circular factor with L=2 is well-defined", {
  factors <- list(A = list(L = 2, type = "circular", l = 1.0))
  K_res <- design_kernel(factors, basis = "cell", normalize = "none")

  expect_equal(dim(K_res$K), c(2, 2))
  expect_true(isSymmetric(K_res$K, tol = 1e-10))

  # Circular distance between positions 1 and 2 with L=2: min(1, 1) = 1
  # So off-diagonal should be exp(-1/(2*1^2)) = exp(-0.5)
  expect_equal(K_res$K[1, 2], exp(-0.5), tolerance = 1e-8)
})
```
  </action>
  <verify>
```bash
Rscript -e "testthat::test_file('tests/testthat/test-design-kernel.R')"
```
All tests pass.
  </verify>
  <done>
- Removed deprecated context() call
- Tests for 1x1 and 2x2 kernels added
- Input validation tests (negative rho, missing L, missing values, unnamed factors)
- kernel_roots edge cases (diagonal, near-zero eigenvalues, asymmetric warning)
- All existing tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify no regressions</name>
  <files></files>
  <action>
Run the complete test suite to verify:
1. All new tests pass
2. No regressions in existing tests
3. Kernel invariants hold across all test cases

```bash
Rscript -e "devtools::test()"
```

Additionally verify kernel reconstruction tolerance is appropriate:
```bash
Rscript -e "
  # Quick reconstruction accuracy check
  K <- dkge::design_kernel(list(A = list(L = 5, type = 'ordinal', l = 1.0)),
                           basis = 'cell', normalize = 'none')\$K
  roots <- dkge::kernel_roots(K)
  max_err <- max(abs(roots\$Khalf %*% roots\$Khalf - K))
  cat('Max reconstruction error:', max_err, '\n')
  stopifnot(max_err < 1e-8)
"
```
  </action>
  <verify>
```bash
Rscript -e "devtools::test()" 2>&1 | tail -20
```
Target: 0 failures, 0 errors.
  </verify>
  <done>
- Full test suite passes
- No regressions introduced
- Kernel reconstruction error < 1e-8 verified
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `test-kernel-invariants.R` exists with property-based tests
2. `test-design-kernel.R` has edge case tests and no context() call
3. `Rscript -e "devtools::test()"` passes all tests
4. `grep -c "test_that" tests/testthat/test-*kernel*.R` shows increased test count
</verification>

<success_criteria>
- [ ] New file `test-kernel-invariants.R` created with >= 100 lines
- [ ] At least 7 property-based tests for mathematical invariants
- [ ] At least 8 new edge case tests in test-design-kernel.R
- [ ] Deprecated context() call removed
- [ ] All tests pass with `devtools::test()`
- [ ] Phase 1 Success Criteria #3 addressed: Kernel output symmetric and PSD
- [ ] Phase 1 Success Criteria #4 addressed: Reconstruction Khalf %*% Khalf = K verified
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-kernel-foundation/01-02-SUMMARY.md`
</output>
