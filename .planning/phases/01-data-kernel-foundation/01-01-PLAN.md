---
phase: 01-data-kernel-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-data.R
  - tests/testthat/test-data-validation.R
autonomous: true

must_haves:
  truths:
    - "dkge_subject() rejects non-matrix inputs with clear error"
    - "dkge_subject() rejects mismatched beta/design dimensions"
    - "dkge_data() rejects empty inputs with informative message"
    - "Effect alignment produces identical results regardless of input order"
    - "NA/Inf values in beta matrices trigger validation errors"
  artifacts:
    - path: "tests/testthat/test-data-validation.R"
      provides: "Input validation edge case tests"
      min_lines: 80
    - path: "tests/testthat/test-data.R"
      provides: "Enhanced data constructor tests"
      contains: "ordering invariance"
  key_links:
    - from: "tests/testthat/test-data-validation.R"
      to: "R/dkge-data.R"
      via: "testthat expect_error assertions"
      pattern: "expect_error.*dkge_subject|expect_error.*dkge_data"
---

<objective>
Harden data constructor input validation with comprehensive edge case tests

Purpose: Ensure `dkge_subject()` and `dkge_data()` reject invalid inputs with clear error messages, and that effect alignment is ordering-invariant. This establishes trust that downstream phases receive correctly structured data.

Output: New test file `test-data-validation.R` with edge case coverage, enhanced `test-data.R` with ordering invariance tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-kernel-foundation/01-RESEARCH.md

@R/dkge-data.R
@tests/testthat/test-data.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test-data-validation.R with edge case tests</name>
  <files>tests/testthat/test-data-validation.R</files>
  <action>
Create a new test file `tests/testthat/test-data-validation.R` that covers input validation edge cases not in the existing `test-data.R`:

1. **dkge_subject edge cases:**
   - Empty beta matrix (0 rows or 0 cols) - should error
   - Non-matrix inputs (data.frame, tibble, vector) - should error with "Unsupported" or type hint
   - Beta with NA values - should error or warn (check current behavior, add test)
   - Beta with Inf values - should error or warn
   - Dimension mismatch: beta rows != design columns - should error with "match"
   - Duplicate effect names in design - verify behavior

2. **dkge_data edge cases:**
   - Empty betas list `list()` - should error
   - Single subject case - should work without error
   - Zero clusters (P = 0) - verify behavior
   - Subjects with completely disjoint effects (no overlap) - verify provenance tracks this

3. **Use parameterized test pattern:**
```r
test_that("dkge_subject rejects invalid inputs with clear messages", {
  design <- matrix(1:10, 5, 2, dimnames = list(NULL, c("eff1", "eff2")))

  cases <- list(
    list(input = data.frame(a = 1:2, b = 3:4), msg = "Unsupported|matrix"),
    list(input = 1:10, msg = "Unsupported|matrix"),
    list(input = matrix(NA_real_, 2, 2, dimnames = list(c("eff1", "eff2"), NULL)), msg = "NA|missing")
  )

  for (case in cases) {
    expect_error(dkge_subject(case$input, design = design), case$msg,
                 info = paste("Input type:", class(case$input)[1]))
  }
})
```

4. **Use withr::local_seed() for any random data generation** (not set.seed() to avoid side effects).

File should have ~80-120 lines covering the gaps identified in RESEARCH.md.
  </action>
  <verify>
```bash
Rscript -e "testthat::test_file('tests/testthat/test-data-validation.R')"
```
All tests should pass or fail expectedly (if revealing existing bugs that need separate fix).
  </verify>
  <done>
- test-data-validation.R exists with >= 80 lines
- Tests for empty inputs, non-matrix inputs, NA/Inf values, dimension mismatches
- All validation tests use expect_error with message patterns
- Tests pass (or document expected failures for future fix)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add effect ordering invariance tests to test-data.R</name>
  <files>tests/testthat/test-data.R</files>
  <action>
Append new tests to `tests/testthat/test-data.R` verifying that effect alignment is ordering-invariant:

1. **Effect ordering invariance test:**
```r
test_that("dkge_data produces identical aligned effects regardless of input order", {
  withr::local_seed(999)
  # Create two subjects with permuted effect ordering
  effects_order1 <- c("eff1", "eff2", "eff3")
  effects_order2 <- c("eff3", "eff1", "eff2")  # permuted

  beta1 <- matrix(rnorm(3 * 10), 3, 10, dimnames = list(effects_order1, NULL))
  beta2 <- matrix(rnorm(3 * 10), 3, 10, dimnames = list(effects_order2, NULL))

  design1 <- matrix(rnorm(20 * 3), 20, 3, dimnames = list(NULL, effects_order1))
  design2 <- matrix(rnorm(20 * 3), 20, 3, dimnames = list(NULL, effects_order2))

  data <- dkge_data(list(beta1, beta2), list(design1, design2))

  # All outputs should have consistent effect ordering
  expect_identical(rownames(data$betas[[1]]), rownames(data$betas[[2]]))
  expect_identical(colnames(data$designs[[1]]), colnames(data$designs[[2]]))
  expect_identical(data$effects, rownames(data$betas[[1]]))
})
```

2. **Subject ordering invariance test:**
```r
test_that("dkge_data is invariant to subject list ordering", {
  withr::local_seed(888)
  beta_a <- matrix(rnorm(3 * 10), 3, 10, dimnames = list(c("e1", "e2", "e3"), NULL))
  beta_b <- matrix(rnorm(3 * 10), 3, 10, dimnames = list(c("e1", "e2", "e3"), NULL))
  design_a <- matrix(rnorm(20 * 3), 20, 3, dimnames = list(NULL, c("e1", "e2", "e3")))
  design_b <- matrix(rnorm(20 * 3), 20, 3, dimnames = list(NULL, c("e1", "e2", "e3")))

  data1 <- dkge_data(list(beta_a, beta_b), list(design_a, design_b),
                     subject_ids = c("A", "B"))
  data2 <- dkge_data(list(beta_b, beta_a), list(design_b, design_a),
                     subject_ids = c("B", "A"))

  # Effects should be identical
  expect_identical(data1$effects, data2$effects)
  # Subject A's data should be identical in both
  idx1 <- which(data1$subject_ids == "A")
  idx2 <- which(data2$subject_ids == "A")
  expect_equal(data1$betas[[idx1]], data2$betas[[idx2]])
})
```

3. **Provenance accuracy test for partial overlap:**
```r
test_that("provenance correctly tracks effect coverage", {
  beta1 <- matrix(1:6, 2, 3, dimnames = list(c("e1", "e2"), NULL))
  beta2 <- matrix(1:6, 2, 3, dimnames = list(c("e2", "e3"), NULL))
  design1 <- matrix(1:10, 5, 2, dimnames = list(NULL, c("e1", "e2")))
  design2 <- matrix(1:10, 5, 2, dimnames = list(NULL, c("e2", "e3")))

  data <- dkge_data(list(beta1, beta2), list(design1, design2))
  prov <- data$provenance

  # e1: only subject 1, e2: both, e3: only subject 2
  expect_equal(prov$pair_counts["e1", "e1"], 1L)
  expect_equal(prov$pair_counts["e2", "e2"], 2L)
  expect_equal(prov$pair_counts["e3", "e3"], 1L)
  expect_equal(prov$pair_counts["e1", "e3"], 0L)  # No subject has both
})
```

Remove any deprecated `context()` calls if present (testthat edition 3 doesn't use them).
  </action>
  <verify>
```bash
Rscript -e "testthat::test_file('tests/testthat/test-data.R')"
```
All tests pass.
  </verify>
  <done>
- test-data.R has >= 3 new tests for ordering invariance
- Effect ordering invariance verified
- Subject ordering invariance verified
- Provenance accuracy for partial overlap verified
- All existing tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify no regressions</name>
  <files></files>
  <action>
Run the complete test suite to verify:
1. All new tests pass
2. No regressions in existing tests
3. Document any unexpected failures

```bash
Rscript -e "devtools::test()"
```

If any tests fail unexpectedly:
- Check if failure is pre-existing (not caused by new tests)
- Document in summary if fix is out of scope for this plan
  </action>
  <verify>
```bash
Rscript -e "devtools::test()" 2>&1 | tail -20
```
Look for "OK", "FAIL", "WARN" counts. Target: 0 failures, 0 errors.
  </verify>
  <done>
- Full test suite passes
- No regressions introduced
- Test count increased from baseline
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `test-data-validation.R` exists with edge case tests
2. `test-data.R` has ordering invariance tests
3. `Rscript -e "devtools::test()"` passes all tests
4. `grep -c "test_that" tests/testthat/test-data*.R` shows increased test count
</verification>

<success_criteria>
- [ ] New file `test-data-validation.R` created with >= 80 lines
- [ ] At least 5 new edge case tests for input validation
- [ ] At least 3 new ordering invariance tests in test-data.R
- [ ] All tests pass with `devtools::test()`
- [ ] Phase 1 Success Criteria #1 addressed: Invalid inputs rejected with clear messages
- [ ] Phase 1 Success Criteria #2 addressed: Effect alignment ordering-invariant
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-kernel-foundation/01-01-SUMMARY.md`
</output>
